#pragma once

#include <string>
#include <vector>
/*
class W_ipconfig
{
public:
	W_ipconfig();
	~W_ipconfig();

	std::vector<std::string> getLocalIpInfo();
	std::vector<std::string> getIpInfo();

private:
	std::vector<std::string> mLocal_IP_Info;
	std::vector<std::string> mIP_Info;
};
*/
